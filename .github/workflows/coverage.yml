name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install coverage codecov

      - name: Run tests with coverage
        run: |
          coverage run --source='apps.child.models' manage.py test
          coverage report
          coverage xml  # Generates the coverage report in XML format

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          files: coverage.xml # The XML file generated by coverage
          token: ${{ secrets.CODECOV_TOKEN }} # Your Codecov token
